<?xml version="1.0" encoding="utf-8"?>
<!--
  Android blocks plain HTTP by default. The local backend runs on http://10.0.2.2:3000
  (the emulator's alias for your Mac), so cleartext is allowed for that host only —
  not app-wide. Add your Mac's LAN IP here too if you test on a physical device.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
</network-security-config>
